spring:
  application:
    name: grpc-metrics-server

grpc:
  server:
    port: 9899

management:
  endpoint:
    metrics:
      enabled: true
  endpoints:
    web:
      exposure:
        include: ["prometheus", "metrics"]
  prometheus:
    metrics:
      export:
        pushgateway:
          enabled: true
